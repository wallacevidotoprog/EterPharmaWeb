import { FormatCpfDirective } from './format-cpf-rg-phone.directive';

describe('FormatCpfRgPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatCpfDirective();
    expect(directive).toBeTruthy();
  });
});
